---
version: 2

registries:
  dockerhub:
    type: docker-registry
    url: https://registry.hub.docker.com
    username: igorferreir4 # ${{ github.repository_owner }}
    password: dckr_pat_M5U5j01Cc-AXajMK4keFhPqqzYg # ${{ secrets.DOCKERHUB_TOKEN }}
    replaces-base: true

updates:
  - package-ecosystem: "docker"
    directory: "/imagens/filebrowsertus"
    registries:
      - dockerhub # Allow version updates for dependencies in this registry
    schedule:
      interval: "daily"
  # - package-ecosystem: "github-actions"
  #   directory: "/"
  #   schedule:
  #     interval: "daily"
