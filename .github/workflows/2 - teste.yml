name: Executar Todos!

on:
  workflow_dispatch:

jobs:
  aspnet:
    name: Build ASP.NET
    uses: ./.github/workflows/aspnet.yml

  steps:
    - name: Checkout repository
      uses: actions/checkout@v3
